# Handoff: Sigil NVDA onboarding — de-GOOG'd the framework, built + blessed the NVDA DCF (nvda/27Q1.1), shipped the history verb; Q2 refresh (+terminal restructure) is the next event

**Date**: 2026-08-26 10:51
**Status**: All landed & fleet empty. NVDA Q2 earnings print TODAY (2026-08-26) — the Q2 refresh is the next work item and its scope is pre-agreed (see Next Steps).

---

## Goal

The captain (Wes, valuation analyst) extended **Sigil** (the AI-maintainable valuation-model system at `projects/sigil`; repo = source of truth, Excel = generated view) from a single ported GOOG model to a real multi-company framework, and onboarded **NVDA from scratch** through the new author path. Everything below is landed on sigil `main` (head `8ef4a49`). This session also set the standing working arrangement: **the captain trusts firstmate for tactical/technical decisions; bring him only judgmental reviews, one by one, each with firstmate's review + concrete recommendation** (recorded in `data/captain.md`).

## Current state (verify with `git -C projects/sigil log --oneline` and `tasks-axi list`)

- **sigil `main` = `8ef4a49`**; new tag **`nvda/27Q1.1`** (= `20c6ce9`, the NVDA bless commit; pre-earnings baseline). GOOG untouched all session (its golden byte-identical; tags 26Q2.2–.4 unchanged).
- **NVDA golden 7-slot signature** ($/share): `consensus 230.35 / base 214.72 / bull 259.45 / bear 52.13 / tail 62.69 / opt_bear 145.00 / opt_bull 323.00`. Full 4-leg gate green across all 7 (recalc 0 errors, parity 299/299, invariants, signature to the cent); suite 111 tests.
- **`base`, `opt_bear`, `opt_bull` are DERIVED market-implied scenarios** (GOOG buffett_be pattern): uniform-λ solve on the consensus growth shape to dated price facts (214.72 @2026-08-21; option-derived 145/323 @2026-08-25, flagged volatile pre-Q2). They **re-solve automatically when the price facts refresh** — expect the signature to move at the Q2 refresh by design.
- **New framework capability landed this session**: `sigil onboard <ticker> [--bless]` (author path + first-bless), permanent `companies/example/` genericity fixture, and `sigil history <ticker> [--driver <n>|--drivers|--facts] [--json]` (evolution view; verified against real GOOG 26Q2.2→.4 and NVDA 27Q1.1 history).
- **Backlog**: `sigil-nvda-buffett` (queued; captain-approved follow-up, deliberately HELD until after the Q2 refresh — it changes the scenario set and would double-churn the signature); `sigil-m4` (GOOG Q3-26 refresh, TIME GATE late Oct). Fleet empty, no open decisions, no holds.

## What was done (chronological, all captain-approved and merged)

1. **Extend-vs-separate decision**: onboarding new companies stays in Sigil (scout evidence backed it). Scout `sigil-onboard-scout` audited engine genericity: entanglement was ~150–250 lines in 2 files (`verify/invariants.py` GOOG node names; `cli.py` 7-scenario order). Report: `data/sigil-onboard-scout/report.md`.
2. **`sigil-degoog`** (Step 1, captain: "Step 1 only"): relocated GOOG's ties to `companies/goog/checks/invariants.py`, per-company signature rendering, robustness fixes, `onboard` verb + bless path, `companies/example/` fixture, surgical SPEC amendment (D1–D7 untouched). Landed `a32bf0d`, no tag (signature-preserving).
3. **NVDA method decisions** (all in `data/sigil-nvda-scoping.md`, sections 1–13 — THE required reading before any NVDA work): consolidated single revenue-growth driver (per-platform superseded); reinvestment derived = ΔRev÷marginal-S2C + wc_pct; capex inverted into a demand-side check netting NVDA-financed capex; R&D NOT capitalized; aggregate OM off the Operating-income line (gains-stripped); simple D&A.
4. **Circular-financing scout** (`sigil-nvda-circfin-scout`): mapped $43.4B non-marketable customer stakes, $30.2B marketable, $27B unfunded commitments, **$105B OpenAI/Portsmouth residual-value guarantee (8-K 2026-08-17, off-balance-sheet)**, $3.5B facility guarantees; ~75–90% of revenue independent of NVDA's own capital. Approved treatment: stakes split by role in the bridge (supplier FV−17.5% tax; customer AT COST − cyclicality haircut), backstop as own line at EXPECTED LOSS (never gross), capex-check netting, and the **double-counting rule** (growth owns demand unwind; margin owns write-downs; stakes at cost own idiosyncratic loss; backstop charged only on insolvency = tail).
5. **`sigil-nvda-build`** (19 commits): facts from the captain's 2,040 XBRL fact cards (copy at `data/sigil-nvda-build/raw-nvda-27Q1/NVDA.jsonl`, sha `cbd064ec`, committed into the repo as `raw/NVDA-val-2027Q1/`); model.py + emit_spec.py; five scenarios; then captain iterations: consensus re-anchored to **real Yahoo revenue consensus** (screenshot preserved; FY2027 $395.41B → Y1 +56.0% by LEVEL-mapping off the LTM base, not the +83% headline rate — avoids double-counting the ramp already in Y0); **base redefined = market-implied** (λ-solve to the dated price fact); Assumptions "Live Output (active scenario)" block; Capital & S2C rebuilt to the GOOG standard after captain rejected v1 as "not informative and lazy"; DCF equity bridge split into **Group 1 standard / Group 2 NVIDIA-specific overlay** with $/share per line (overlay ≈ +$2.16/share in base — nearly value-neutral outside tail); option-implied expectations ladder (145/214.72/323 → λ 0.64/0.94/1.28, implied Y1 growth 35.9/52.9/71.4%) then **promoted to scenarios opt_bear/opt_bull**. Captain blessed the 7-slot signature; landed `20c6ce9`, tagged `nvda/27Q1.1`.
6. **`sigil-history`** verb: signature timeline per tag (deltas + reasons) + per-driver evolution with rationales. Landed `8ef4a49`.
7. **Terminal-block analysis (decision recorded, NOT yet executed)**: terminal ROIC 32% is a fudge dial (only sets reinvestment g/ROIC=13.4% vs ~2% implied by marginal S2C 30×; measured Y4 ROIC ~200%+); the genuinely aggressive dial is the 55% terminal margin (≈2× the TV lever of ROIC). Agreed restructure: assume terminal margin + terminal capital turnover, DERIVE ROIC/reinvestment, add a coherence invariant. **Captain: "don't touch it pre-print" — execute INSIDE the Q2 refresh.** Full reasoning in scoping notes §13.

## What didn't work / gotchas (beyond the 26-08-20 handoff's, which all still apply)

- **A worker parked on `needs-decision` triggers endless stale/wedge escalations** (observed up to escalation 8 with demand-deep-inspection). Reconcile with `fm-crew-state.sh` (claude-hook busy source) + one pane peek; a parked-at-captain-gate worker with a clean summary and empty composer is healthy. Known upstream gap (issue #2713 / PR #2738).
- **`cd` into a project clone is blocked** by a firstmate PreToolUse hook — use `git -C <dir>` or a subshell `(cd <dir> && ...)`.
- **`fm-teardown.sh`/`fm-merge-local.sh` take a task id, not `--help`** — read script headers with `sed -n '1,40p'`.
- **`fm-decision-hold.sh hold --reason` refuses parentheses** (tasks-axi contract) — strip them.
- **Workbook inspection without touching project venvs**: `uv run --no-project --with openpyxl python <script>` against the xlsx copy in `data/` (dump script kept at the scratchpad; trivial to rewrite).
- **The captain reviews the workbook from `data/sigil-nvda-build/nvda-27Q1-dcf.xlsx`** — refresh it with `cp` from the build after every rebuild; he keeps it open in Excel (`~$` lock file appears there; harmless, it's outside the repo).
- **Captain supplies market data as screenshots + dated instructions** (Yahoo consensus screenshot preserved at `data/sigil-nvda-build/yahoo-consensus-screenshot.png`; price 214.72 as-of 8/21 — his date, not the record date; option strikes 145/323 @8/25). Transcribe exactly into a steer file, preserve the image, record as dated facts.
- **His raw data folder** was `~/Documents/Documents - MBP M2/260818_NVDA-27Q1-val/raw/NVDA.jsonl` (he cited it as "raw/nvda-27Q1/" — search his Documents when a cited path doesn't exist).
- The build worker ended at ~78% of a 1M context after 20 commits and many captain iterations — a Q2 refresh gets a FRESH worker; brief it with the scoping notes + this handoff's next-steps section.

## Key files & locations

| File | Role |
|------|------|
| `projects/sigil/` | The project. `main`=`8ef4a49`, tag `nvda/27Q1.1`. `companies/{goog,nvda,example}/`, `SPEC.md`, `framework/` |
| `data/sigil-nvda-scoping.md` | **THE NVDA method-decision record (§1–13), all captain-approved. Required reading for the Q2 refresh** |
| `data/sigil-nvda-circfin-scout/report.md` | Circular-financing evidence + treatment (Parts 1/3/4) with filing citations |
| `data/sigil-onboard-scout/report.md` | Engine-genericity audit (spec for the landed de-GOOG refactor) |
| `data/sigil-nvda-build/` | Build task artifacts: brief + steer files (`consensus-revision.md`, `base-rebuild.md`, `capital-sheet-rebuild.md`, `bridge-restructure.md` — good templates), raw cards copy, Yahoo screenshot, captain's workbook review copy |
| `data/sigil-*/decision-*.md` | Durable captain decisions (onboarding-plan, circfin-treatment) — both resolved/closed |
| `data/captain.md` | Standing orders incl. NEW: judgmental-reviews-only style + workbook format taste (dense live tables, no prose pointers) |
| `data/learnings.md` | Prior gotchas (hand-edit workflow, stale-wake noise) — still accurate |

## Next steps

1. **NVDA Q2 refresh** (the captain will initiate by sending data; earnings print 2026-08-26). Scope is pre-agreed — dispatch ONE fresh worker (claude / claude-opus-4-8 / xhigh, local-only, yolo off) with a brief covering: (a) refresh dated price fact + option strikes + Yahoo consensus (captain supplies; derived base/opt_bear/opt_bull re-solve automatically); (b) Q2 FY2027 10-Q facts when he provides cards (the Q2 10-Q also carries the Portsmouth guarantee exhibit — update `facts/contingent_commitments.yaml`); (c) **the terminal-block restructure per scoping §13** (assume terminal margin + capital turns, derive ROIC, coherence invariant) — signature-moving, so one re-bless with per-scenario deltas vs `nvda/27Q1.1`, shown to the captain as a table; (d) tag on approval (likely `nvda/27Q2.1`). Use `sigil history nvda` output in the captain relay — this is the evolution tracker's first real entry.
2. **After the refresh settles**: dispatch `sigil-nvda-buffett` (queued; short now — the λ-solver pattern exists in base/opt scenarios).
3. **Late Oct**: `sigil-m4` GOOG Q3-26 refresh (unchanged from prior handoff; ask for source files first).
4. Any new format request: his GOOG Capital & S2C sheet is the canonical taste anchor; follow the captain.md format-taste entry and the hand-edit procedure from the prior handoff.

## Context & notes

- **Bless and merge are ALWAYS separate captain words** — he blesses a signature, then separately says "land". First-bless of any new company is his (recorded ownership understanding). Never both from one approval.
- The needs-decision/`--resolve-key` flow worked flawlessly all session: workers open `needs-decision [key=x]`, firstmate answers with `fm-send --resolve-key x`, superseded gates get a new key (first-bless → -2 → -3 → -4).
- He answers fast, in few words ("bless and go on the history verb", "land"), and asks "do you understand?" when direction is subtle — confirm by restating, then act.
- Reply `Captain, shipshape.` to routine no-action wakes; batch everything else.
- All previous handoff gotchas (Excel locks blocking merges, `fm-spawn` needing `projects/sigil` not bare `sigil`, WATCHER DOWN banners cosmetic under herdr) remain true and bit again this session.
